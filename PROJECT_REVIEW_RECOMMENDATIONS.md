1. AI总结页生成AI总结的时候，我发现在合并不同区块的摘要的时候，中间显示内容会变成待生成，这一点要修复一下。
   - 落地状态：已处理。长视频分块摘要完成后会立即展示已生成分段内容，进入合并阶段时在最终合并正文出现前继续保留分段摘要，避免正文回到空状态。
2. 视频洞察页的回答里面为什么有很多框呀？比如一部分相对粗体的文字，你要把它框起来，然后下面一段换行了以后好像又有一个框把这下面的一行又框了起来。你是不是在style里面覆盖了某个元素的 style.
   - 落地状态：已处理。聊天气泡样式改为只作用于消息根节点的直接段落，不再框住 Markdown 内部段落或加粗内容。
3. 一图流里面的重新生成好像并没有真实的重新生成，而只是把之前的生成的图片加载了出来。
   - 落地状态：已处理。一图流重新生成会传入 force 标记，绕过已有 JSON 和图片缓存，重新调用生成链路。
4. 设置界面有一个面板宽度的设置，这一点去掉，以后的所有Tab的面板宽度可以通过界面左侧的Resize来进行，并保存在一个地方。这样的话，再次加载不会需要重复的去调整宽度。
   - 落地状态：已处理。设置页移除面板宽度字段，面板侧边增加 resize handle，所有 Tab 共用并持久化同一个宽度配置。
5. 请设置界面的请求模式，这里默认用自动的。然后把这两个文本模型和生图模型里面的请求模式选项都去掉，不要让用户自己去选，让整个项目统一采用现在自动的优先流式fetch，必要时回退GM的方式。
   - 落地状态：已处理。设置页移除文本和图片请求模式选择，保存和加载配置时统一强制为 auto。
6. 去掉生图模型区域里面的启用生图模型这个form field，默认是启用的。
   - 落地状态：已处理。设置页移除启用生图模型字段，保存和加载配置时默认启用；是否调用生图模型由一图流模式决定。
7. 设置界面吸底的两个Button，检查文本配置和检查生图配置，这两个不要放在吸底的区域里面，要放在上面文本模型和生图模型设置区域Header的右侧。这里这两个检查配置的按钮文本都为连通性测试。
   - 落地状态：已处理。两个按钮已移动到对应 section header 右侧，统一按钮文案为“连通性测试”。
8. 这里这个检查文本配置和检查生图配置，这两个功能是不是还不够全面？它是不是只是做了一次文本的检查？能不能通过调用一次简单的API来实现检查？当然，这个具体的实现的Button上面需要有明确的tooltip，用户鼠标hover上去的时候，要告诉用户会实际发送一次请求。如果这次请求成功，会谈一个相应的提示。如果请求不成功，要把相应请求不成功的原因。也放在整体页面的toast上
   - 落地状态：已处理。连通性测试会实际发送轻量文本或图片 API 请求；按钮 tooltip 明确提示会发起请求；成功和失败都会通过全局 toast 反馈。
9. 以上的所有bug fix都完成以后，我需要你在设置里面增加一个新的设置，叫做总结语言。这里这样排版，现在设置里面的界面section改名叫做语言section。然后第一个里面下面有两个dropdown，分别是界面的显示语言，和字幕获取及总结的语言，这个你取一个比较合适的名字吧。这两个Dropdown list下面都只有两个选项，分别是中文和英文。那我需要你对哔哩哔哩站点上的英文总结进行一定的兼容。比如目前应该是指去获取中文的字幕。那我需要你当用户选择了用英文来做总结的话，那你应该会去尝试去获取英文的字幕，注意这里所有的prompt是不是应该需要中英两个版本？
   - 落地状态：已处理。设置页语言 section 包含“界面显示语言”和“字幕获取及总结语言”；英文总结会优先尝试英文字幕，摘要、分块/合并、视频洞察和一图流 JSON 内置 prompt 已补充英文版本。切换总结语言时会清除旧字幕选择，避免继续复用之前自动选中的中文字幕。
